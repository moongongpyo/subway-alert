spring:
  application:
    name: subway-alert

server:
  port: ${PORT:8080}
  shutdown: graceful

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: never

info:
  app:
    name: Subway Alert
    description: Local coordinator for subway delay detection and verification agents
